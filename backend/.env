import os
from sqlalchemy import create_engine

DATABASE_URL = os.gentenv("DATABASE_URL")
engine = create_engine(DATABASE_URL, pool_pre_ping=True)
SECRET_KEY = ledgerpro-super-secret-key
ALGORITHM = HS256
ACCESS_TOKEN_EXPIRE_MINUTES = 30